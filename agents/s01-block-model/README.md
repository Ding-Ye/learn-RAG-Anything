# s01 · block-model

The opening chapter. Defines the typed vocabulary every later chapter
uses (`Block`, `BlockKind`, `Document`) and shows the data shape with a
hand-written sample document.

```bash
go run ./agents/s01-block-model
go test  ./agents/s01-block-model
```

Read the explainer at [`docs/en/s01-block-model.md`](../../docs/en/s01-block-model.md)
or [`docs/zh/s01-block-model.md`](../../docs/zh/s01-block-model.md).
